# Core runtime: pipeline + Streamlit app. Pinned for reproducibility.
# Upgrade deliberately, then re-pin: pip install -U <pkg> && pip freeze > requirements.txt
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
lightgbm==4.5.0
shap==0.46.0
joblib==1.4.2
streamlit==1.40.2
